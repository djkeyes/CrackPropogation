package crackSim.core;

/**
 * Grid class to store adjacent FEM elements, akin to a cellular automaton.
 * Each propogator keeps a grid updated at its own timespeed, but multiple grids can refer to a single BackingGrid, which stores the original layour.
 */
public class Grid {
	
	private BackingGrid backingGrid;
	
	
}
